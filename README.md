# cedar-presentation

An HTML presentation arguing for purchasing STK rustic channel cedar siding lumber now rather than later, based on price projection analysis.

## Structure

- `index.html` - Main presentation (uses Tailwind CSS, Chart.js)
- `docs/` - Reference materials and PDF reports

## Usage

Open `index.html` in a browser to view the presentation.
